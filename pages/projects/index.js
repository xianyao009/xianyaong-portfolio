import ProjectsContainer from "../../components/container/ProjectsContainer";
import Content from "../../components/container/Content";

const projects = () => {
  return (
    <>
      <Content content={<ProjectsContainer />} />
    </>
  );
};

export default projects;
